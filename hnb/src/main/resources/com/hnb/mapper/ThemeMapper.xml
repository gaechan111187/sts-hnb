<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.hnb.mapper.ThemeMapper">
 	<select id="count" resultType="int">
 		SELECT COUNT(*) FROM BOARD
 	</select>
	<select id="countByKeyword" resultType="int" parameterType="com.hnb.global.Command">
	   	SELECT COUNT(*) AS count 
	   	FROM THEME
	   	WHERE ${column} LIKE '%' || #{key} || '%'
	</select>
	<select id="getList" resultType="com.hnb.theme.ThemeVO" parameterType="com.hnb.theme.ThemeVO">
	   	SELECT 
	   		THEME_NO AS themeNO,
	   		THEME_NAME AS themeName
   		FROM THEME
	</select>
	<select id="searchByKeyword" resultType="com.hnb.theme.ThemeVO" parameterType="com.hnb.global.Command">
 		SELECT 
 			THEME_NO AS themeNO,
	   		THEME_NAME AS themeName
 		FROM THEME WHERE ${column} LIKE '%' ${key} '%' ORDER BY THEME_NAME DESC
 	</select>
 	<select id="searchByNumber" resultType="com.hnb.theme.ThemeVO" parameterType="string">
		SELECT
			THEME_NO AS themeNO 
		FROM THEME WHERE THEME_NO=${themeNo]	
 	</select>
 	<update id="change" parameterType="com.hnb.theme.ThemeVO">
 		UPDATE THEME SET 
	 		THEME_NAME = #{themeName}, 
 		WHERE THEME_NO = #{themeNO}
 	</update>
 	<delete id="remove" parameterType="string">
 		DELETE FROM THEME WHERE THEME_NO = #{themeNO}
 	</delete>
</mapper>